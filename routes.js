// Global
const HOME = "/";

// Search

const SEARCH = "/search";
const COMPARE = "/compare";
const CART = "/cart";

const DART = "https://opendart.fss.or.kr";
const API = "/api";

const routes = {
  home: HOME,
  search: SEARCH,
  compare: COMPARE,
  cart: CART,
  dart: DART,
  api: API,
};

export default routes;
