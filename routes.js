// Global
const HOME = "/";

// Search

const SEARCH = "/search";
const COMPARE = "/compare";

const API = "/api";

const routes = {
  home: HOME,
  search: SEARCH,
  compare: COMPARE,
  api: API,
};

export default routes;
