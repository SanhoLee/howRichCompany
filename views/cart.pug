extends layout/main.pug
include mixins/cartBlock

block fwanggu
        h1 CART items
        ul.cart__wrapper
            each corp_data in cartCorps
                +cartBlock({
                    corp_name:corp_data.corp_name,
                    corp_number:corp_data.corp_code
                })
        button.button.result_button#jsResultBtn(name="resultBtn" formaction=routes.home) Show Result ✅